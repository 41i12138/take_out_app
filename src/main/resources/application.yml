server:
  port: 8080

spring:
  # 数据库连接信息
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/take_out?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&useSSL=false&autoReconnect=true&failOverReadOnly=false
    username: root
    password: llw2382257
  redis:
    host: localhost
    port: 6379
    database: 0
#  cache:
#    redis:
#      time-to-live: 180000 # 设置缓存过期世界为30分钟
#    jedis:
#      # Redis连接池配置
#      pool:
#        max-active: #最大连接数
#        max-wait: 1ms #连接池最大阻塞等待时间
#        max-idle: 4 #连接池中的最大空闲连接
#        min-idle: 0 #连接池中的最小空闲连接

mybatis-plus:
  configuration:
    #在映射实体或者属性时，将数据库中表名和字段名中的下划线去掉，按照驼峰命名法映射
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: assign_id

takeout:
  path: D:\Code\IdeaProjects\take_out_app\images\